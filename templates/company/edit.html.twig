{% extends 'base.html.twig' %}

{% block title %}{{ 'company.billing_information'|trans }}{% endblock %}

{% block body %}
    <h1 class="mb-10 flex justify-start items-center text-xl space-x-3">
        <a href="{{ path('app_dash_board',{'company': company.id}) }}" class="text-decoration-none">
            <svg class="size-8">
                <use href="#arrow-left-circle"/>
            </svg>
        </a>
        <span>{{ 'company.billing_information'|trans }}</span>
    </h1>

    {{ include('company/_form.html.twig', {'button_label': 'button.update'}) }}

{% endblock %}
