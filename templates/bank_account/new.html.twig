{% extends 'base.html.twig' %}

{% block title %}New BankAccount{% endblock %}

{% block body %}
    <h1 class="mb-10 flex justify-start items-center text-xl space-x-3">
        <a href="{{ path('app_setting_account_index', {'company':company.id}) }}" class="text-decoration-none">
            <svg class="size-8">
                <use href="#arrow-left-circle"/>
            </svg>
        </a>
        <span>Create new BankAccount</span>
    </h1>

    {{ include('bank_account/_form.html.twig') }}

{% endblock %}
