{% extends 'base.html.twig' %}

{% block title %}Hello DocumentNumbersController!{% endblock %}

{% block body %}
    <h1 class="mb-5">
        <a href="{{ path('app_dash_board',{'company': company.publicId}) }}" class="text-decoration-none">
            <svg class="size-8">
                <use href="#arrow-left-circle"/>
            </svg>
        </a>
        Upravit
    </h1>

    {{ include('document_numbers/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
