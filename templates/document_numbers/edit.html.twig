{% extends 'base.html.twig' %}

{% block title %}{{ 'document_number.edit_number'|trans }} {{ documentType.name }}{% endblock %}

{% block body %}
    <h1 class="mb-10 flex justify-start items-center text-xl space-x-3">
        <a href="{{ path('app_setting_document_numbers_index',{'company':company.publicId}) }}">
            <svg class="size-8">
                <use href="#arrow-left-circle"/>
            </svg>
        </a>
        <span>{{ 'document_number.edit_number'|trans }} {{ documentType.name }}</span></h1>

    {{ include('document_numbers/_form.html.twig', {'button_label': 'button.update'|trans}) }}

    <a href="{{ path('app_setting_document_numbers_index', {'company':company.publicId}) }}"></a>

{% endblock %}
