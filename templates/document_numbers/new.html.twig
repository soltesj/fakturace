{% extends 'base.html.twig' %}

{% block title %}{{ 'document_number.add_number'|trans }} {{ documentType.name }}{% endblock %}

{% block body %}
    <h1 class="mb-10 flex justify-start items-center text-xl space-x-3">
        <a href="{{ path('app_setting_document_numbers_index',{'company':company.publicId}) }}"
           class="text-decoration-none">
            <svg class="size-8">
                <use href="#arrow-left-circle"/>
            </svg>
        </a>
        {{ 'document_number.add_number'|trans }} {{ documentType.name }}</h1>

    {{ include('document_numbers/_form.html.twig', {'button_label': 'button.save'|trans}) }}

{% endblock %}
