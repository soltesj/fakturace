{% extends 'base.html.twig' %}

{% block title %}Edit Customer{% endblock %}

{% block body %}
    <h1 class="mb-10 flex justify-start items-center text-xl space-x-3">
        <a href="{{ path('app_customer_index', {'company': company.publicId}) }}" class="text-decoration-none">
            <svg class="size-8">
                <use href="#arrow-left-circle"/>
            </svg>
        </a>
        <span>Upravit</span>
    </h1>

    {{ include('customer/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
