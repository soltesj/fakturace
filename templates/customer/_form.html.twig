{{ form_start(form,{'attr':{
    'data-document--new-customer-target':'form',
    'data-action':'submit->document--new-customer#submit'
}}) }}
<div {{ stimulus_controller('customer/ico-autofill',{}) }}>
    <div {{ stimulus_target('customer/ico-autofill','alertBox') }}></div>
    <div class="flex justify-between">
        <div class="">
            {{ form_row(form.country, { attr: { 'data-customer--ico-autofill-target': 'countryInput' }}) }}

            <div class="mb-1 grid grid-cols-[12rem_1fr]">
            {{ form_label(form.companyNumber) }}

            {{ form_widget(form.companyNumber, { attr: { 'data-customer--ico-autofill-target': 'companyNumberInput' }}) }}
                <div class="col-span-4 col-start-2">
                    <button class=" bg-background-secondary disabled:bg-gray-500 flex items-center justify-center p-2"
                            type="button" id="button-addon"
                    {{ stimulus_action('customer--ico-autofill','lookup') }}
                    {{ stimulus_target('customer--ico-autofill','lookupButton') }}
                    >Auto fill
                        <svg class="animate-spin size-5 ml-3 hidden" {{ stimulus_target('customer--ico-autofill','buttonSpinner') }}>
                    <use href="#spinner"/>
                </svg>
                        <svg class="size-5 ml-3 text-secondary" {{ stimulus_target('customer--ico-autofill','buttonLabel') }}>
                    <use href="#share"/>
                </svg>
            </button>
                </div>
        </div>


        {{ form_row(form.name, { attr: { 'data-customer--ico-autofill-target': 'nameInput' } }) }}
        {{ form_row(form.contact) }}
        {{ form_row(form.street, { attr: { 'data-customer--ico-autofill-target': 'streetInput' } }) }}
        {{ form_row(form.town, { attr: { 'data-customer--ico-autofill-target': 'townInput' } }) }}
        {{ form_row(form.isVatPayer,{ attr: { 'data-customer--ico-autofill-target': 'isVatPayerInput' } }) }}
    </div>
        <div class="">
        {{ form_row(form.vatNumber, { attr: { 'data-customer--ico-autofill-target': 'vatNumberInput' } }) }}
        {{ form_row(form.email) }}
        {{ form_row(form.phone) }}
        {{ form_row(form.houseNumber, { attr: { 'data-customer--ico-autofill-target': 'houseNumberInput' } }) }}
        {{ form_row(form.zipcode, { attr: { 'data-customer--ico-autofill-target': 'zipcodeInput' } }) }}
        {{ form_row(form.bankAccount) }}
        {{ form_row(form.web) }}
        </div>
    </div>
    <button class="w-full mt-10 p-2 bg-green-700 hover:bg-green-500 text-green-100">{{ button_label|default('button.save')|trans }}</button>
</div>
{{ form_end(form) }}
