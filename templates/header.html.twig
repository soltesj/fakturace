<header class="bg-gray-900 text-white border-b border-gray-800 mb-4">
    <div class="container mx-auto flex flex-wrap items-center justify-between">
        <a href="{{ path('app_home') }}" class="flex items-center text-white text-xl font-semibold">
            <!-- Logo nebo SVG -->
            <svg class="w-10 h-8 mr-2" role="img" aria-label="Bootstrap">
                <use href="#bootstrap"/>
            </svg>
            MojeApp
        </a>

        <nav class="flex space-x-6 text-sm mt-4 lg:mt-0">
            <a href="{{ path('app_home') }}"
               class="{% if app.current_route=='app_home' %}text-gray-400{% else %}text-white hover:text-gray-400{% endif %} flex items-center space-x-1">
                <svg class="w-5 h-5" fill="currentColor">
                    <use href="#home"/>
                </svg>
                <span>Domů</span>
            </a>
            <a href="#" class="text-white hover:text-gray-400 flex items-center space-x-1">
                <svg class="w-5 h-5" fill="currentColor">
                    <use href="#price"/>
                </svg>
                <span>Cena</span>
            </a>
            <a href="#" class="text-white hover:text-gray-400 flex items-center space-x-1">
                <svg class="w-5 h-5" fill="currentColor">
                    <use href="#question"/>
                </svg>
                <span>FAQ</span>
            </a>
        </nav>

        <div class="flex space-x-4 text-sm mt-4 lg:mt-0">
            <a href="{{ path('app_login') }}"
               class="{% if app.current_route=='app_login' %}text-gray-400{% else %}text-white hover:text-gray-400{% endif %} flex items-center space-x-1">
                <svg class="w-5 h-5" fill="currentColor">
                    <use href="#person"/>
                </svg>
                <span>Přihlásit</span>
            </a>
            <a href="{{ path('app_register') }}"
               class="{% if app.current_route=='app_register' %}text-gray-400{% else %}text-white hover:text-gray-400{% endif %} flex items-center space-x-1">
                <svg class="w-5 h-5" fill="currentColor">
                    <use href="#person-add"/>
                </svg>
                <span>Registrovat</span>
            </a>
        </div>
    </div>
</header>